#ifndef ANALYSIS_H
#define ANALYSIS_H
void WriteResults(double *x, double *p);
void GetXP(double *x, double *p, double *x_now, double *p_now, int it);
  // not needed in this homework set
// double TotalEnergy(double *x, double *p, int it);
#endif